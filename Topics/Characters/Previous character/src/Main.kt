fun main() {
    var a: Char = readln().first()
    var b: Char = readln().first()
    var c: Char = readln().first()
    var d: Char = readln().first()

    println(--a)
    println(--b)
    println(--c)
    println(--d)
}